apiVersion: v1
kind: Service
metadata:
  name: archetype-backend-demo
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: archetype-backend-demo
  ports:
    - port: 8080
      targetPort: 80
