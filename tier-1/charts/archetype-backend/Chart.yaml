apiVersion: v2
name: archetype-backend
description: Baseline backend service chart (Deployment + Service) maintained by platform engineering
type: application
# publish-chart.yaml overrides this with --version/--app-version at publish
# time; the OCIRepository tag Flux tracks (e.g. 0.1.1, 0.1.2) reflects what was
# actually published, not this file's version.
version: 0.1.0
appVersion: "1.0.0"
