# Dedicated application workspace namespace for showcase workloads.
# Not yet label-scoped for governance opt-in — clusterpolicy-spicedb-authz.yaml
# is hardcoded to this namespace name. Label-based opt-in arrives in tier 5.
apiVersion: v1
kind: Namespace
metadata:
  name: apps
